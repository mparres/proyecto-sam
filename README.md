# proyecto-sam
Analyzes a SAM file and reports alignment statistics

# requirements
- Python 3.11
- uv
- Nextflow 25.10.4

# usage
Run with Nextflow:
   nextflow run main.nf --sam sample.sam

Or run with Python: 
   uv run python main.py sample.sam

